<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BowieD.Unturned.NPCMaker.Forms"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" x:Class="BowieD.Unturned.NPCMaker.Forms.AssetPicker_Window"
    xmlns:xaml="clr-namespace:BowieD.Unturned.NPCMaker.XAML"    
    mc:Ignorable="d"
        Title="{xaml:Localize INTERFACE_AssetPicker_Title}" SizeToContent="Height" Width="400">
    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="{DynamicResource Scale}" ScaleY="{DynamicResource Scale}"/>
        </Grid.LayoutTransform>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer MinHeight="400" Grid.Row="0" Margin="5">
            <StackPanel Name="stack"/>
        </ScrollViewer>
        <GroupBox Margin="5" Grid.Row="1" Header="Filter">
            <StackPanel Orientation="Vertical">
                <TextBox Name="filter_name" Margin="10" mah:TextBoxHelper.Watermark="Search..." />
                <CheckBox Name="filter_origin_unturned" Margin="10" Content="Unturned" IsChecked="True"/>
                <CheckBox Name="filter_origin_workshop" Margin="10" Content="Workshop" IsChecked="True"/>
                <CheckBox Name="filter_origin_project" Margin="10" Content="Project" IsChecked="True"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
